# FinalAxis Mobile Backup

Private backup of the local AXIS mobile workspace.

Included:
- `apps/mobile` copied from the local machine, including local-only files
- `packages/theme`

Excluded:
- `node_modules`
